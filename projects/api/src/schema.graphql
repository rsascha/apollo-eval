type Query {
  movies: [Movie]
  actors: [Actor]
  movie(id: ID!): Movie
  actor(id: ID!): Actor
  randomWord: String!
}

type Mutation {
  addMovie(input: AddMovieInput!): Movie!
}

input AddMovieInput {
  title: String!
  actorIds: [ID!]!
}

type Movie {
  id: ID!
  title: String!
  actors: [Actor!]!
}

type Actor {
  id: ID!
  name: String!
  movies: [Movie!]!
}
