type Query {
  movies: [Movie]
  actors: [Actor]
  movie(id: ID!): Movie
  #   actor(id: ID!): Actor
}

type Movie {
  id: ID!
  title: String!
  actors: [Actor!]!
}

type Actor {
  id: ID!
  name: String!
  movies: [Movie!]!
}
